import Config from "../Config";

function LoginPage(){
    let loginURL = Config.getAPIServerURL();
    return (
        <div className="login-page bg-gray-900 min-h-screen min-w-full">
            
        </div>
    );
}

export default LoginPage;