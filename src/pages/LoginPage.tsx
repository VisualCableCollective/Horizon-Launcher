import Config from "../Config";

function LoginPage(){
    let loginURL = Config.getAPIServerURL();
    return (
        <div className="login-page bg-gradient-1 min-h-screen min-w-full">
            
        </div>
    );
}

export default LoginPage;